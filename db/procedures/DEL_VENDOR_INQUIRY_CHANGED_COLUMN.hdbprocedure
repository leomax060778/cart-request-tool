PROCEDURE "CART_REQUEST_TOOL"."xscartrequesttool.db.procedures::DEL_VENDOR_INQUIRY_CHANGED_COLUMN" (
	IN in_vendor_inquiry_id BIGINT,
	OUT out_result BIGINT
 )
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER
	DEFAULT SCHEMA "CART_REQUEST_TOOL"
	AS
BEGIN
	DELETE FROM "CART_REQUEST_TOOL"."VENDOR_INQUIRY_CHANGED_COLUMN"
	WHERE VENDOR_INQUIRY_ID = in_vendor_inquiry_id;

	SELECT ::ROWCOUNT INTO out_result FROM DUMMY;
END;