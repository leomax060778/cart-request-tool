PROCEDURE "CART_REQUEST_TOOL"."xscartrequesttool.db.procedures::DEL_EXTEND_VENDOR_REQUEST_CHANGED_COLUMN" (
	IN in_extend_vendor_request_id BIGINT,
	OUT out_result BIGINT
 )
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER
	DEFAULT SCHEMA "CART_REQUEST_TOOL"
	AS
BEGIN
	DELETE FROM "CART_REQUEST_TOOL"."EXTEND_VENDOR_REQUEST_CHANGED_COLUMN"
	WHERE EXTEND_VENDOR_REQUEST_ID = in_extend_vendor_request_id;

	SELECT ::ROWCOUNT INTO out_result FROM DUMMY;
END;