CREATE COLUMN TABLE CONFIGURATION
(
	CONFIGURATION_ID BIGINT NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
   	ENVIRONMENT_ID BIGINT NOT NULL,
	ALLOWED_USERS NVARCHAR(255) NOT NULL,
	MAINTENANCE TINYINT DEFAULT 1,
	
	CREATED_DATE_TZ TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	MODIFIED_DATE_TZ TIMESTAMP,
	CREATED_USER_ID BIGINT NOT NULL,
	MODIFIED_USER_ID BIGINT,
	ENABLED TINYINT DEFAULT 1, 
	DELETED TINYINT DEFAULT 0,

);

ALTER TABLE "CART_REQUEST_TOOL"."CONFIGURATION" ADD FOREIGN KEY ( "ENVIRONMENT_ID" ) REFERENCES "CART_REQUEST_TOOL"."ENVIRONMENT" ("ENVIRONMENT_ID") ON UPDATE RESTRICT ON DELETE RESTRICT;