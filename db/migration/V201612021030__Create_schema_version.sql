CREATE COLUMN TABLE "CART_REQUEST_TOOL".SCHEMA_VERSION
(
	SCHEMA_VERSION_ID BIGINT NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	VERSION NVARCHAR(100) NOT NULL,
	DESCRIPTION NVARCHAR(255),
	SCRIPT NVARCHAR(1000),
	INSTALLED_ON_TZ TIMESTAMP DEFAULT CURRENT_TIMESTAMP     
);

CREATE UNIQUE INDEX idxschversion ON "CART_REQUEST_TOOL".SCHEMA_VERSION(VERSION);

-- *************************************************************************************
-- Update schema version
INSERT INTO SCHEMA_VERSION(VERSION, DESCRIPTION, SCRIPT) 
VALUES('V2.0.0-01', 'Create table schema version and unique index', 'V201612021030__Create_schema_version.sql');

COMMIT;